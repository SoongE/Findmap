stages:
  - test

variables:
  INNER_NAME: "RHCSKY"

test_flutter:
  tags:
    - runner
  stage: test
  script:
    - echo INNER_NAME=$INNER_NAME

test_nodejs:
  tags:
    - runner
  stage: test
  script:
    - echo INNER_NAME=$INNER_NAME

test_flask:
  tags:
    - runner
  stage: test
  before_script:
    - cd flask-api
    - export PATH=$PATH:/opt/conda/bin
    - python -m pip install -r requirements.txt
  script:
    - echo INNER_NAME=$INNER_NAME